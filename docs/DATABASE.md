# Room database

## Offline reference catalog

BMW F30 320d is stored under catalog key `REF-BMW-F30-320D` with note **OFFLINE REFERENCE CATALOG**.  
It must **not** be presented as a live VIN or live ECU online map.

Live sessions store real VIN (or `UNKNOWN`) in `test_sessions` and raw traffic in `diagnostic_logs`.

## Tables

vehicles, ecus, dtc_codes, standards, test_sessions, diagnostic_logs, knowledge_cache
